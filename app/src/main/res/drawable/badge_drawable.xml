<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@drawable/badge_background" />

    <!-- Text shape for the badge count -->
    <item
        android:drawable="@drawable/badge_text"
        android:right="6dp"
    android:top="6dp" />

    <!-- Text for the badge count -->
    <item
        android:id="@android:id/text1"
        android:gravity="center"
        android:textSize="10sp"
    android:textColor="#FF0000" /> <!-- Change text color as desired -->
</layer-list>